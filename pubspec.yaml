name: app_medagenda
description: "Agendamentos para clinicas médicas"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.6
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  intl:
  carousel_slider:
  flutter_dotenv:
  dartz:
  equatable: ^2.0.5
  confetti:
  timezone: 
  flutter_localizations:
    sdk: flutter
  firebase_core: ^2.27.0
  firebase_analytics: ^10.8.9

assets:
  - .env
  # - fonts/
  - images

flutter:
  uses-material-design: true
  # fonts:
